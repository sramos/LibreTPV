  <%= selector "Materia", "producto", "materia_id",
               (@materias||[]).map {|u| [u.nombre,u.id]},
               { valor: @valor_materia_defecto,
                 tipo: 'x15',
                 vacio: false } %>
